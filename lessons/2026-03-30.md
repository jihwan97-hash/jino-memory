# lessons — 2026-03-30

## skills/ gitignore 제거 — 스킬 버전관리 전략 변경

**문제**: `.gitignore`에 `skills/`가 등록되어 있어서 크론이 isolated 세션에서 실행될 때 스킬 스크립트를 찾지 못함. 특히 `kb-review.js`, `digest.py` 등 커스텀 스크립트가 git에 없으니 cron이 매번 실패.

**배경**: 2026-03-29 레슨에서 `skills/`를 gitignore에 추가했는데, 당시 이유는 OpenClaw 심링크가 git add -A에 의해 덮어쓰여지는 문제였음. 하지만 그 결과로 커스텀 스크립트도 함께 추적 불가 상태가 됨.

**해결**:
- `.gitignore`에서 `skills/` 라인 제거
- `skills/` 전체 git 추적 재개
- 심링크 충돌은 `git add -f` + 주의해서 커밋하는 방식으로 대응

**교훈**:
- OpenClaw 기본 스킬(~/.nvm/... 경로)과 커스텀 스크립트(skills/ 내)를 구분해야 함
- 커스텀 스크립트는 반드시 git 추적 대상 — 없으면 cron isolated 세션에서 못 찾음
- `git add -A`가 문제라면 gitignore보다 cron 페이로드의 `git add <specific-paths>` 전환이 더 나은 해결책

## daily-digest 스킬 신규 생성

**사실**: `daily-digest` 스킬이 cron에 등록되어 있었지만 `digest.py` 파일이 존재하지 않았음. `.gitignore`로 인해 생성했어도 유실되었거나, 애초에 만들어진 적 없었을 가능성.

**해결**: `skills/daily-digest/scripts/digest.py` 직접 작성
- RSS 기반 팟캐스트 수집 (Lex Fridman, a16z, Tim Ferriss)
- Brave API 키 있으면 트위터 웹 검색 보완
- 출력: `jino-memory/research/podcasts/YYYY-MM-DD-digest.md`

**교훈**: cron에 등록된 스크립트 경로는 실제 파일 존재 여부를 주기적으로 확인할 것. 파일이 없으면 cron은 조용히 실패하거나 에이전트가 직접 웹에서 수집하는 방식으로 fallback함.

## astinclaw.pw 배포 방법

**사실**: Vercel로 배포. git remote 없음 — `vercel deploy --prod` 직접 실행.
- 프로젝트: `/Users/astin/Projects/astinclaw-web`
- Vercel 프로젝트: `prj_yddAm2LQzKN26pDwMI75SSztIxmW`
- 도메인: `https://astinclaw.pw`

**교훈**: git push 안 된다고 배포 실패로 착각하지 말 것. Vercel CLI 키 갖고 있음 — 다음부터 블로그 글 올릴 때 바로 `vercel deploy --prod` 실행.

## kb-review.js 신규 생성

**사실**: `kb-review` cron이 `skills/brain-memory/scripts/kb-review.js`를 실행하도록 설정되어 있었지만 해당 파일이 없었음. GitHub openclaw 레포에도 존재하지 않음 — 완전 커스텀 스크립트.

**해결**: ClawVault daily 파일(facts/decisions/goals/inbox)을 스캔하여 신규 항목 추출 + confidence/importance 기반 티어(HOT/WARM/COOL) 분류하는 스크립트 직접 작성.

**교훈**: GitHub에 없는 스크립트를 cron에 등록할 때는 반드시 git 추적 + 스크립트 생성을 함께 진행할 것.
- [lesson|c=0.84|i=0.90] 06:00 [main] toolresult: [main c5d4685] lesson: skills gitignore 제거 + daily-digest/kb-review 교훈 (2026-03-30) 1 file changed, 36 insertions(+) create mode 100644 lessons/2026-03-30.md To https://github.com/jihwan97-hash/jino-memory.git ! [rejected] main -> main (fetch first) error: fail

## skills 페이지 날아간 원인 및 복구

**문제**: `vercel deploy --prod`로 로컬 `/Users/astin/Projects/astinclaw-web/` 기준 배포했더니 skills 페이지가 사라짐.

**원인**: 
- 이전 배포는 Python 스크립트로 `/tmp/astinclaw-src/src/` 기준 전체 파일 업로드 방식이었음
- 로컬 git 폴더에는 `skills/` 디렉토리가 없었음 (git에 커밋되지 않은 상태)
- `vercel deploy`는 로컬 파일만 올리므로 skills 페이지가 누락된 채 덮어씌워짐

**복구 방법**:
- Vercel API로 이전 배포(`dpl_DA7haqPLqoyCTYPrV4KkJnFk4ywe`)에서 파일 다운로드
- `GET /v7/deployments/{id}/files/{uid}` → base64 디코딩 → 로컬 저장
- git commit 후 `vercel deploy --prod` 재배포

**교훈**:
1. `vercel deploy --prod` 전에 로컬 폴더 구조 확인 필수 — 배포된 페이지가 로컬에 없으면 사라짐
2. 모든 페이지(skills, activity 등)는 반드시 로컬 git에도 있어야 함
3. nav 메뉴도 페이지 추가/삭제 시 전체 파일 일괄 업데이트 필요
4. 이전 배포 복구 시: Vercel API `GET /v6/deployments/{id}/files` → 파일 트리 조회 → `GET /v7/deployments/{id}/files/{uid}` 개별 다운로드
- [lesson|c=0.72|i=0.45] 11:00 [main] toolresult: [main 161c920] lesson: skills 페이지 유실 원인 및 복구 방법 추가 1 file changed, 20 insertions(+) From https://github.com/jihwan97-hash/jino-memory * branch main -> FETCH_HEAD [[Current]] branch main is up to date. To https://github.com/jihwan97-hash/jino-memory.git e189fe3..161
