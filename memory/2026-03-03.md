# 2026-03-03 Daily Memory

## 주요 작업

### Claude Code CLI 설치 & 인증
- `npm install -g @anthropic-ai/claude-code` → v2.1.63 설치
- 인증: `~/.claude/.credentials.json`에 `claudeAiOauth` 키로 저장 (oauthAccount가 아님!)
- `claude auth status` → loggedIn: true 확인

### tmux-claudecode 스킬 생성
- `/Users/astin/.jinobot/clawd/skills/tmux-claudecode/SKILL.md` + `scripts/create-session.js` + `scripts/check-session.js`
- tmux 설치 (apt-get), 소켓: `/tmp/openclaw-tmux-sockets/openclaw.sock`
- opencode 대신 `claude --dangerously-skip-permissions` 사용

### 크론 에러 (미해결)
- `brain-memory`, `email-summary`, `morning-briefing` 3개 — OAuth 토큰 취소 에러
- `HTTP 403 permission_error: OAuth token has been revoked`
- `auto-study`, `portfolio-research`는 정상
- 해결: 형이 `openclaw setup` 재인증 필요

### Telegram 스트리밍 설정
- `streaming: "block"` 으로 변경 (`/root/.openclaw/openclaw.json`)
- 중간에 `partial`로 돌아갔다가 다시 `block`으로 재설정

### 캘린더 변경사항
- **기본 캘린더: `astin@hashed.com`** (기존 jihwan 캘린더 → 형이 수정 가능하도록)
- `GOOGLE_CALENDAR_ID=astin@hashed.com` 환경변수 사용
- `--visibility` 옵션 추가 (public|private|default)
- Google Meet 링크 생성: PATCH API + `conferenceDataVersion=1`

### 오늘 생성된 일정 (astin 캘린더)
- 11:00 HVF1,2 Exit 실무 Sync (Meet: meet.google.com/kbr-tyaw-ayd)
- 15:00 강남파라곤 투룸 B동 오피스텔라인
- 16:00 삼성동24 탐방
- 17:00 피부과 방문 (gkswlghks118 초대)
- 19:00 [TODO] 주노, mythical 업데이트
- 19:30 [TODO] howmuch 작성
- 20:00 [TODO] 두잇 회신

### SOUL.md 업데이트
- **Knowledge Pipeline** 추가: 링크 → 읽기 → 형에게 확인 → 저장 → 푸시
- **Proof of Work** 추가: 증거 없이 "완료" 금지
- 저장 경로: `warm-memory/knowledge-base/short-term/`
- 기억 티어: short-term → mid-term → long-term

### 지식베이스 구축
- `warm-memory/knowledge-base/` 폴더 구조 생성
- kb-001: Nathan AI 기억시스템 아티클
- kb-002: geopolythink 코스피 하반기 수혜 시나리오
- 서브에이전트로 기존 리서치 일괄 임포트 중 (portfolio 50개 포함)

### 자동차번호등록 일정 메모 추가
- 이벤트 ID: `bgbddbilp9acqqi04s69l21q28`
- 삼성카드 다이렉트오토 차량번호 등록 안내 추가 (10일 이내 등록)

## 기술 메모
- `claudeAiOauth` 키가 Claude Code CLI 인증 파일의 정확한 키명
- Claude Code credentials: `~/.claude/.credentials.json` (not `~/.claude.json`)
- Google Meet: `PATCH /events/{id}?conferenceDataVersion=1` + `conferenceSolutionKey: hangoutsMeet`
- 캘린더 API access token은 oauth2.googleapis.com/token으로 직접 발급 가능
